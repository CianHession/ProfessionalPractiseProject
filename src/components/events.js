import React from 'react';

export class Events extends React.Component{
    render(){
        return(
            <div>
                <h1>This is the page that will display a timetable of events</h1>
            </div>
        );
    }
}